{
  "name": "post-messenger",
  "version": "0.1.0",
  "description": "A wrapper of window.postMessage for cross-document communication.",
  "main": "lib/index.js",
  "scripts": {
    "test": "jest",
    "coveralls": "cat ./coverage/lcov.info | coveralls",
    "build:umd": "rimraf ./dist && cross-env NODE_ENV=production webpack",
    "build:lib": "rimraf ./lib && cross-env NODE_ENV=production babel src -d lib",
    "build": "npm run build:umd && npm run build:lib && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:hustcc/post-messenger.git"
  },
  "keywords": [
    "postMessage",
    "iframe",
    "post-messenger",
    "cross-document"
  ],
  "author": "ProtoTeam",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-jest": "^21.2.0",
    "babel-loader": "^7.1.2",
    "babel-plugin-add-module-exports": "^0.2.1",
    "babel-plugin-transform-class-properties": "^6.24.1",
    "babel-preset-env": "^1.6.1",
    "coveralls": "^3.0.0",
    "cross-env": "^5.1.3",
    "jest": "^21.2.1",
    "jsdom": "^11.5.1",
    "mitt": "^1.1.3",
    "rimraf": "^2.6.2",
    "webpack": "^3.10.0"
  },
  "jest": {
    "setupFiles": [
      "<rootDir>/__tests__/setup.js",
      "<rootDir>/__tests__/jest-postmessage-mock.js"
    ],
    "testRegex": "(/__tests__/.*(test|spec))\\.(js)$",
    "moduleFileExtensions": [
      "js",
      "json"
    ],
    "transform": {
      "^.+\\.js?$": "babel-jest"
    },
    "collectCoverage": true,
    "collectCoverageFrom": [
      "src/**/*.{js}",
      "!**/node_modules/**",
      "!**/vendor/**"
    ],
    "moduleDirectories": [
      "node_modules",
      "src"
    ]
  }
}
